<?php
/**
 * @deprecated since 3.18, to be removed in 3.19. (See MAGE-1579.)
 *             Part of the legacy backend-facet-rendering subsystem.
 *             Loaded only when algoliasearch_instant/instant/backend_rendering_enable is '1', which is
 *             unreachable since the admin field was removed from system.xml and can never be set.
 *             Backend rendering is now provided by the optional Algolia_SearchAdapter module.
 * @see https://github.com/algolia/algoliasearch-adapter-magento-2
 *
 * @var \Magento\Framework\Escaper $escaper
 */

if ($block->canShowBlock()): ?>
    <div class="block filter algolia-filter-list" id="layered-filter-block" data-mage-init='{"collapsible":{"openedState": "active", "collapsible": true, "active": false, "collateral": { "openedState": "filter-active", "element": "body" } }}'>
        <?php $filtered = count($block->getLayer()->getState()->getFilters()) ?>
        <div class="block-title filter-title" data-count="<?= $escaper->escapeHtmlAttr($filtered) ?>">
            <strong data-role="title"><?= $escaper->escapeHtml(__('Shop By')) ?></strong>
        </div>

        <div class="block-content filter-content">
            <?php if ($block->getLayer()->getState()->getFilters()): ?>
                <div class="block-actions filter-actions">
                    <a href="<?= /* @escapeNotVerified */ $block->getClearUrl() ?>" class="action clear filter-clear"><span><?= /* @escapeNotVerified */ __('Clear All') ?></span></a>
                </div>
            <?php endif; ?>
            <?= $block->getChildHtml('state') ?>

            <?php $wrapOptions = false; ?>
            <?php $range = implode(' ', range(0, count($block->getFilters()))); ?>
            <?php foreach ($block->getFilters() as $filter): ?>
                <?php if ($filter->getItemsCount()): ?>
                    <?php if (!$wrapOptions): ?>
                        <div class="filter-options" id="narrow-by-list" data-role="content" data-mage-init='{"accordion":{"openedState": "active","active" : "<?= $escaper->escapeHtmlAttr($range); ?>", "collapsible": "true", "multipleCollapsible": "false"}}'>
                            <?php $wrapOptions = true; ?>
                    <?php endif; ?>
                    <div data-role="collapsible" class="filter-options-item">
                        <div data-role="title" class="filter-options-title"><?= $escaper->escapeHtml(__($filter->getName())) ?></div>
                        <div data-role="content" class="filter-options-content"><?= $escaper->escapeHtml($block->getChildBlock('renderer')->render($filter)) ?></div>
                    </div>
                <?php endif; ?>
            <?php endforeach; ?>
            <?php if ($wrapOptions): ?>
                </div>
            <?php else: ?>
                <?php if (class_exists('\Magento\Framework\View\Helper\SecureHtmlRenderer')): ?>
                    <?php
                    /** @var \Magento\Framework\View\Helper\SecureHtmlRenderer $secureRenderer */
                    $scriptString = "require([
                            'jquery'
                        ], function ($) {
                            $('#layered-filter-block').addClass('filter-no-options');
                        });";

                    /* @noEscape */ $secureRenderer->renderTag('script', [], $scriptString, false); ?>
                <?php else: ?>
                    <script>
                        require([
                            'jquery'
                        ], function ($) {
                            $('#layered-filter-block').addClass('filter-no-options');
                        });
                    </script>
                <?php endif; ?>
            <?php endif; ?>
        </div>
    </div>
<?php endif; ?>
